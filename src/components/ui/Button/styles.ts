import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  main: {
    borderRadius: 13,
    borderWidth: 2,
    borderColor: '#FFFFFF',
    alignItems: 'center',
    justifyContent: 'center',
    padding: 5,
    flexDirection: 'row',
    gap: 8,
  },
})
